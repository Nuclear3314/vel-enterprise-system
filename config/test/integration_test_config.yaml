test_config:
  api:
    base_url: "http://localhost:8000"
    timeout: 30
    retry_attempts: 3
  
  database:
    host: "localhost"
    port: 5432
    database: "test_db"
    
  cache:
    host: "localhost"
    port: 6379
    db: 1